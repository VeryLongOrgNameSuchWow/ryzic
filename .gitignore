# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv
venv/

# Tool caches
.pytest_cache/
.ruff_cache/
.tox/
.coverage
htmlcov/

# IDE / OS
.DS_Store
.vscode/
.idea/

# Local config (NEVER commit — see incident log)
.env
.env.*

# Audio + playlist cache (LRU, local default)
.cache/

# SQLite ancillary files
*.sqlite-journal
*.sqlite-wal
*.sqlite-shm

# Coverage tooling artefacts
.coverage
.coverage.*
htmlcov/

# Claude Code agent worktrees + task state
.claude/

# Generic credential patterns (defense-in-depth — never expected, never wanted)
*.pem
*.key
*.crt
*.kdbx
id_rsa*
id_ed25519*
id_ecdsa*
secrets/
secrets.json
credentials.json

# YouTube cookies — README's "Opt-in: YouTube cookies" recipe places
# youtube-cookies.txt at the repo root. Never commit a real cookies file.
cookies.txt
youtube-cookies.txt
*.cookies.txt
